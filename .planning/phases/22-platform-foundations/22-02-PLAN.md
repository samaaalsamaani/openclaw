---
phase: 22-platform-foundations
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/bluebubbles/package.json
  - extensions/copilot-proxy/package.json
  - extensions/diagnostics-otel/package.json
  - extensions/discord/package.json
  - extensions/feishu/package.json
  - extensions/google-gemini-cli-auth/package.json
  - extensions/imessage/package.json
  - extensions/irc/package.json
  - extensions/line/package.json
  - extensions/matrix/package.json
  - extensions/mattermost/package.json
  - extensions/memory-lancedb/package.json
  - extensions/minimax-portal-auth/package.json
  - extensions/msteams/package.json
  - extensions/nextcloud-talk/package.json
  - extensions/nostr/package.json
  - extensions/signal/package.json
  - extensions/slack/package.json
  - extensions/synology-chat/package.json
  - extensions/telegram/package.json
  - extensions/tlon/package.json
  - extensions/twitch/package.json
  - extensions/voice-call/package.json
  - extensions/whatsapp/package.json
  - extensions/zalo/package.json
  - extensions/zalouser/package.json
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "better-sqlite3 is confirmed in root package.json dependencies (not devDependencies)"
    - "tsdown.config.ts external: ['better-sqlite3'] is confirmed at lines 108 and 116"
    - "All 26 affected extensions have peerDependencies: { 'openclaw': '>=2026.1.26' } added alongside their existing devDependencies workspace:* entry"
    - "The devDependencies workspace:* entry is NOT removed from any extension — it stays for local monorepo dev"
    - "googlechat and memory-core remain unchanged (already have correct pattern)"
    - "llm-task, lobster, open-prose are not touched (no openclaw dep at all)"
    - "CONCERNS.md is updated to reflect that better-sqlite3 is already in production deps"
  artifacts:
    - path: extensions/discord/package.json
      provides: "Representative example of peerDependencies addition"
      contains: "peerDependencies"
    - path: extensions/telegram/package.json
      provides: "Representative example of peerDependencies addition"
      contains: "peerDependencies"
    - path: .planning/codebase/CONCERNS.md
      provides: "Updated to reflect better-sqlite3 already in production deps"
      contains: "better-sqlite3"
  key_links:
    - from: extensions/*/package.json
      to: package.json
      via: "peerDependencies semver range >=2026.1.26"
      pattern: "peerDependencies"
---

<objective>
Verify better-sqlite3 is correctly placed in production dependencies (it is — correct the stale CONCERNS.md), add peerDependencies entries to 26 extensions that currently only declare openclaw as a devDependency, and document both fixes.

Purpose: Extensions published to npm must declare openclaw as a peerDependency so package managers can resolve the correct version. The devDependencies workspace:\* entry serves local monorepo development but is opaque to external npm consumers. Without peerDependencies, an extension installed via npm has no way to express its minimum openclaw version requirement. The better-sqlite3 verification closes the stale CONCERNS.md entry that claims it is in devDependencies.

Output: 26 extension package.json files updated, CONCERNS.md corrected.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-platform-foundations/22-RESEARCH.md

<interfaces>
<!-- Canonical extension peerDependencies pattern — verified from extensions/memory-core/package.json and extensions/googlechat/package.json -->

BEFORE (current state of 26 affected extensions):

```json
{
  "devDependencies": {
    "openclaw": "workspace:*"
  }
}
```

AFTER (target state — add peerDependencies, keep devDependencies unchanged):

```json
{
  "devDependencies": {
    "openclaw": "workspace:*"
  },
  "peerDependencies": {
    "openclaw": ">=2026.1.26"
  }
}
```

Semver constraint rationale: `>=2026.1.26` is the minimum version where the current plugin API is stable. Current openclaw version is `2026.2.24`. Use `>=2026.1.26` to match the memory-core and googlechat canonical examples.

Extensions already correct (DO NOT TOUCH):

- extensions/memory-core — already has both devDependencies and peerDependencies
- extensions/googlechat — already has both devDependencies and peerDependencies

Extensions exempt (NO openclaw dep at all — DO NOT TOUCH):

- extensions/llm-task
- extensions/lobster
- extensions/open-prose

better-sqlite3 verification (root package.json — confirmed correct):

```json
"dependencies": {
  "better-sqlite3": "^12.6.2"
}
```

tsdown.config.ts external setting (confirmed at lines 108 and 116):

```typescript
external: ["better-sqlite3"];
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify better-sqlite3 production placement and update CONCERNS.md</name>
  <files>
    .planning/codebase/CONCERNS.md
  </files>
  <action>
Step 1 — Verify the current state (do not assume, confirm from files):
- Read `package.json` (root) and confirm `better-sqlite3` appears in `dependencies` (not `devDependencies`).
- Run `node -e "require('better-sqlite3')" 2>&1` to confirm the package resolves at runtime.
- Read `tsdown.config.ts` and confirm `external: ["better-sqlite3"]` appears at lines 108 and 116.
- Scan sub-packages: run `grep -r '"better-sqlite3"' extensions/*/package.json 2>/dev/null` — there should be no results (only root has the dep).

Step 2 — Read CONCERNS.md in full.

Step 3 — Update CONCERNS.md:

- Find the section about `better-sqlite3` (likely under "Dependency" or "Production" concerns).
- Update the entry to reflect the verified state: `better-sqlite3` is in root `dependencies` (confirmed 2026-03-02), `tsdown.config.ts` marks it `external` at lines 108 and 116, no sub-packages carry a duplicate dep.
- If CONCERNS.md has a status field or checkbox, mark this concern as resolved.
- Add a note: "Verified in Phase 22. CONCERNS.md was written from a prior state. No code change required."

Step 4 — Verify OBS-07 health dashboard dependency:

- The STATE.md mentions "OBS-07 health dashboard blocked by better-sqlite3 bundling in tsdown". Confirm that the existing `external: ["better-sqlite3"]` setting already unblocks this. Note in CONCERNS.md if OBS-07 is now unblocked by this configuration (no code change needed, just documentation).
  </action>
  <verify>
  <automated>node -e "require('better-sqlite3'); console.log('ok')" && grep -c '"better-sqlite3"' package.json</automated>
  </verify>
  <done>CONCERNS.md updated to reflect better-sqlite3 is already in production deps. Node require test passes. grep confirms single dep location in root package.json.</done>
  </task>

<task type="auto">
  <name>Task 2: Add peerDependencies to all 26 affected extensions</name>
  <files>
    extensions/bluebubbles/package.json
    extensions/copilot-proxy/package.json
    extensions/diagnostics-otel/package.json
    extensions/discord/package.json
    extensions/feishu/package.json
    extensions/google-gemini-cli-auth/package.json
    extensions/imessage/package.json
    extensions/irc/package.json
    extensions/line/package.json
    extensions/matrix/package.json
    extensions/mattermost/package.json
    extensions/memory-lancedb/package.json
    extensions/minimax-portal-auth/package.json
    extensions/msteams/package.json
    extensions/nextcloud-talk/package.json
    extensions/nostr/package.json
    extensions/signal/package.json
    extensions/slack/package.json
    extensions/synology-chat/package.json
    extensions/telegram/package.json
    extensions/tlon/package.json
    extensions/twitch/package.json
    extensions/voice-call/package.json
    extensions/whatsapp/package.json
    extensions/zalo/package.json
    extensions/zalouser/package.json
  </files>
  <action>
For each of the 26 extension package.json files listed above:

1. Read the file.
2. Confirm it has `"devDependencies": { "openclaw": "workspace:*" }`.
3. Confirm it does NOT already have `peerDependencies` with openclaw (if it already does, skip it).
4. Add a `peerDependencies` block: `"peerDependencies": { "openclaw": ">=2026.1.26" }`.
5. Place `peerDependencies` adjacent to (after) `devDependencies` in the JSON structure.
6. Write the updated file.

Critical rules:

- Do NOT remove or modify the existing `"devDependencies": { "openclaw": "workspace:*" }` entry — it is needed for local monorepo development.
- Do NOT use `workspace:*` in peerDependencies — use the real semver range `">=2026.1.26"`.
- Preserve all existing JSON formatting style (indentation, key ordering).
- If an extension already has a `peerDependencies` block for other packages, ADD `"openclaw": ">=2026.1.26"` to the existing block rather than creating a duplicate.

After editing all 26 files, run:

```
pnpm install --frozen-lockfile 2>&1 | tail -5
```

This confirms pnpm accepts the updated package.json files without lockfile conflicts. If `--frozen-lockfile` fails because new peerDependencies requires lockfile update, run `pnpm install` once (without the flag) to update the lockfile, then note this in the summary.
</action>
<verify>
<automated>node -e "const exts=['bluebubbles','copilot-proxy','diagnostics-otel','discord','feishu','google-gemini-cli-auth','imessage','irc','line','matrix','mattermost','memory-lancedb','minimax-portal-auth','msteams','nextcloud-talk','nostr','signal','slack','synology-chat','telegram','tlon','twitch','voice-call','whatsapp','zalo','zalouser']; const fs=require('fs'); let pass=true; for(const e of exts){ const p=JSON.parse(fs.readFileSync('extensions/'+e+'/package.json','utf8')); if(!p.peerDependencies?.openclaw){console.error('MISSING peerDep:',e);pass=false;} if(!p.devDependencies?.openclaw){console.error('REMOVED devDep:',e);pass=false;} } if(pass)console.log('All 26 extensions have correct peerDependencies AND devDependencies');"</automated>
</verify>
<done>All 26 extension package.json files have peerDependencies: { "openclaw": ">=2026.1.26" } added alongside the existing devDependencies workspace:\* entry. memory-core and googlechat unchanged. pnpm install resolves without errors.</done>
</task>

</tasks>

<verification>
1. `node -e "require('better-sqlite3'); console.log('better-sqlite3 resolves OK')"` — passes.
2. `grep -c "peerDependencies" extensions/discord/package.json extensions/telegram/package.json extensions/slack/package.json` — each shows 1.
3. `grep "workspace:\*" extensions/discord/package.json extensions/telegram/package.json` — devDependencies workspace:* still present.
4. `grep -l "peerDependencies" extensions/memory-core/package.json extensions/googlechat/package.json` — both already had it, still present.
5. `cat .planning/codebase/CONCERNS.md | grep -A3 "better-sqlite3"` — shows updated/resolved status.
6. `pnpm tsgo` — zero type errors (package.json changes don't affect TS compilation, but confirm no regressions).
</verification>

<success_criteria>

- better-sqlite3 confirmed in root dependencies — no code change needed, CONCERNS.md corrected
- 26 extension package.json files each have peerDependencies: { "openclaw": ">=2026.1.26" }
- No existing devDependencies entries removed from any extension
- memory-core, googlechat, llm-task, lobster, open-prose left untouched
- CONCERNS.md updated to reflect verified state (not stale planning-doc state)
- pnpm install resolves cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/22-platform-foundations/22-02-SUMMARY.md` with:
- Confirmation of better-sqlite3 status (it was already correct — what was actually verified)
- List of 26 extensions updated with peerDependencies
- Any extensions that were skipped and why (e.g., already had the correct pattern)
- CONCERNS.md change summary
- Whether pnpm install ran with or without --frozen-lockfile
</output>
